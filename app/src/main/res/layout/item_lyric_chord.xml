<!-- item_lyric_chord.xml -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="0dp">

    <!-- Línea de acordes: Roboto Mono Semibold para destacar sobre la letra -->
    <TextView
        android:id="@+id/tvChordLine"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:fontFamily="@font/roboto_mono_semibold"
        android:textSize="14sp"
        android:includeFontPadding="false"
        android:padding="0dp"
        android:lineSpacingExtra="0dp"
        android:letterSpacing="0"
        android:textIsSelectable="false"
        android:textAllCaps="false"/>

    <!-- Línea de letra: Roboto Mono Regular para lectura cómoda -->
    <TextView
        android:id="@+id/tvLyricLine"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:fontFamily="@font/roboto_mono_regular"
        android:textSize="14sp"
        android:layout_marginTop="2dp"/>
</LinearLayout>
